# transcode_av1.py 新手開發者指南 (Essential)

## 1. 先理解這支程式在做什麼
這支程式不是單純「轉檔」而已，它同時處理：
- 檔案掃描
- ffmpeg 指令組裝
- 即時進度顯示
- 成功/跳過/失敗狀態管理
- 原檔安全移動與輸出落地

你可以把它想成一個「批次轉檔任務管理器」。

## 2. 新手最常改的設定
直接在 `CONFIG` 內調整：
- `SOURCE_DIR`、`RECYCLE_DIR`
- `ENCODER`
- `CQ_VALUE`
- `SIZE_THRESHOLD`
- `SUBTITLE_MODE`

建議第一次先用小資料夾測試，確認流程正確再跑整個 NAS。

## 3. 一次看懂主流程（讀碼路徑）
建議閱讀順序：
1. `main()`：理解整體控制
2. `process_one()`：理解單檔成敗條件
3. `build_ffmpeg_cmd()`：理解 ffmpeg 參數來源
4. `parse_progress_line()` + `make_panel()`：理解進度顯示

## 4. 你需要知道的三個關鍵觀念
1. 為什麼用暫存檔？
   - 避免轉到一半就覆蓋原始檔，降低資料遺失風險。
2. 為什麼有 `SIZE_THRESHOLD`？
   - 轉碼不是一定會變小，不符效益就不替換原檔。
3. 為什麼要寫 state？
   - 長任務中斷後可續跑，不會重複處理已完成檔案。

## 5. 觀察執行結果
- **Rich 即時面板**：顯示總進度、ETA、fps、speed，「目前檔案」欄位顯示**完整路徑**，方便確認正在處理哪個 NAS 路徑上的檔案。
- **每檔完成行**：每個檔案結束後會在面板下方輸出：
  - `✓ \\nas\path\file.mp4`（成功）
  - `→ \\nas\path\file.avi`（跳過）
  - `✗ \\nas\path\file.mov`（失敗）
- **`log.txt`**：完整記錄每檔結果與 reason，是排查問題的主要依據。
- **`transcode_state.json`**：記錄每個檔案的最終狀態，決定下次跑時哪些要跳過。

## 6. 新手安全修改清單
- 只改 `CONFIG`：風險最低
- 要改 ffmpeg 參數時：
  - 先對單一檔案測試
  - 確認輸出可播放、字幕與音訊都正常
- 要改檔案搬移邏輯時：
  - 必做備份
  - 先在測試目錄演練成功/失敗/中斷情境

## 7. 常見錯誤與快速排查
- `ffmpeg` 找不到：
  - 檢查 PATH 或把 `FFMPEG_PATH` 改成完整路徑
- GPU 編碼不可用：
  - 先切 `ENCODER="libsvtav1"` 驗證流程
- 字幕報錯：
  - 先切 `SUBTITLE_MODE="ignore"`
- 任務中斷後不確定進度：
  - 看 `transcode_state.json` 的 `processed`
  - 或直接重跑，已完成的會自動跳過

## 8. 建議的第一個開發任務
適合新手的改動：
- 新增「最小檔案大小過濾」功能（例如小於 100MB 就跳過）

實作建議：
1. 在 `CONFIG` 新增參數（如 `MIN_SIZE_MB`）
2. 在 `process_one()` 前段加入檔案大小判斷
3. 回傳 `skipped` 並寫清楚 reason
4. 確認 state 與 log 會記錄這個跳過原因

## 9. 協作時的基本習慣
- 每次改動先寫下「改動目的」與「風險點」
- 優先保持錯誤訊息可讀（reason 要具體）
- 避免一次改太多處，拆小步驟比較好除錯
- 改完至少做一次小規模實跑驗證

## 10. 文件與隱私安全
- 範例設定優先使用相對路徑（如 `.\\log.txt`），避免暴露個人工作目錄。
- 預設路徑若需寫在程式中，建議以 `Path(__file__).resolve().parent` 推導。
- 提交前先檢查 staged 檔案，確認沒有明文憑證與本機隱私路徑。
